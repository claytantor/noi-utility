package com.noi.utility.data; import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.net.MalformedURLException;import java.net.URL;public class URLReader {        //String mp_PageString;    private URL tUrl;    private String pageurl;                 public URLReader(String url) throws MalformedURLException {        this.tUrl = new URL(url);        this.pageurl = url;            }        public URLReader(URL murl) {        this.tUrl = murl;        this.pageurl = murl.getPath();          }            public String getPage() throws IOException    {        StringBuffer pageBuf = new StringBuffer();               BufferedReader in = new BufferedReader(new InputStreamReader(this.tUrl.openStream()));        String inputLine = "";        while ((inputLine = in.readLine()) != null)        {               pageBuf.append(inputLine);        }        in.close();	                       return pageBuf.toString();            }} 